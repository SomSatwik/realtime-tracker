# Example environment variables for Real-Time Tracker
# Default port the app will listen on
PORT=4000

# Add other environment variables below (e.g. DB connection strings)
# MONGODB_URI=mongodb://user:pass@host:port/dbname
